# SFX Library · huashu-design

> All generated by the ElevenLabs Sound Generation API, Apple-keynote-grade audio quality.
> Production-grade SFX asset library covering all Huashu animation / presentation / product demo scenarios.

**Asset location**: `assets/sfx/<category>/<name>.mp3`
**Total**: 37 SFX (30 batch-generated + 7 retained from v7b)
**Generation model**: ElevenLabs Sound Generation API (prompt_influence 0.4)
**Audio quality**: 44.1kHz MP3, Apple-keynote-grade clarity, no extra reverb

---

## Directory structure

```
assets/sfx/
├── keyboard/      type, type-fast, delete-key, space-tap, enter
├── ui/            click, click-soft, focus, hover-subtle, tap-finger, toggle-on
├── transition/    whoosh, whoosh-fast, swipe-horizontal, slide-in, dissolve
├── container/     card-snap, card-flip, stack-collapse, modal-open
├── feedback/      success-chime, error-tone, notification-pop, achievement
├── progress/      loading-tick, complete-done, generate-start
├── impact/        logo-reveal, logo-reveal-v2, brand-stamp, drop-thud
├── magic/         sparkle, ai-process, transform
└── terminal/      command-execute, output-appear, cursor-blink
```

---

## Quick index

### ⌨️ Keyboard (keyboard input)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/keyboard/type.mp3` | 0.5s | Single keystroke (mechanical keyboard single key) | mechanical keyboard single key press |
| `sfx/keyboard/type-fast.mp3` | 1.5s | Continuous fast typing (demo of typing prompts) | fast continuous typing rhythm, apple magic keyboard |
| `sfx/keyboard/delete-key.mp3` | 0.5s | Backspace | single backspace key, low pitched thud |
| `sfx/keyboard/space-tap.mp3` | 0.5s | Spacebar light tap | soft spacebar tap, wide flat |
| `sfx/keyboard/enter.mp3` | 0.5s | Enter key (retained from v7b) | enter key press, crisp tactile |

### 🎯 UI (interface interaction)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/ui/click.mp3` | 0.5s | Standard UI click (retained from v7b) | crisp modern interface click |
| `sfx/ui/click-soft.mp3` | 0.5s | Soft UI click (secondary buttons / links) | soft gentle button click, mid pitched |
| `sfx/ui/focus.mp3` | 0.5s | Element focus / selection (retained from v7b) | subtle focus tone, element highlight |
| `sfx/ui/hover-subtle.mp3` | 0.5s | Hover hint (microsecond feedback) | barely audible tick, air whisper |
| `sfx/ui/tap-finger.mp3` | 0.5s | Mobile tap (iOS interface) | finger tap on touchscreen, muted thud |
| `sfx/ui/toggle-on.mp3` | 0.5s | Toggle switch on | ios toggle switch flip, satisfying click |

### 🌊 Transition (transitions)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/transition/whoosh.mp3` | 0.5s | Standard whoosh (retained from v7b) | air whoosh transition |
| `sfx/transition/whoosh-fast.mp3` | 0.6s | Fast whoosh (title flash, tab switching) | quick fast air whoosh, cinematic |
| `sfx/transition/swipe-horizontal.mp3` | 0.7s | Horizontal swipe (carousel, tab switching) | smooth left-to-right air movement |
| `sfx/transition/slide-in.mp3` | 0.6s | Element slide in (side panel, drawer) | smooth soft whoosh with arrival |
| `sfx/transition/dissolve.mp3` | 0.8s | Soft dissolve (image fade out / fade in) | soft dissolve, airy shimmer |

### 🃏 Container (cards / containers)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/container/card-snap.mp3` | 0.5s | Card snap into place (retained from v7b) | card snap into place |
| `sfx/container/card-flip.mp3` | 0.7s | Card flip (front-back switch) | playing card flip, crisp snap |
| `sfx/container/stack-collapse.mp3` | 0.8s | Stack collapse (list aggregation) | cards stacking, paper taps collapsing |
| `sfx/container/modal-open.mp3` | 0.6s | Modal opens | modal popping open, whoosh + thud |

### 🔔 Feedback (notifications / feedback)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/feedback/success-chime.mp3` | 1.0s | Success indication (payment success, task complete) | two ascending bell tones, ios-style |
| `sfx/feedback/error-tone.mp3` | 0.7s | Error indication (warning, failure) | descending two-note warning, soft |
| `sfx/feedback/notification-pop.mp3` | 0.6s | Message popup (toast, notification) | notification bloop, ios message alert |
| `sfx/feedback/achievement.mp3` | 1.5s | Achievement unlocked (milestone, badge) | triumphant rising arpeggio, game-style |

### ⏳ Progress (progress / state)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/progress/loading-tick.mp3` | 0.5s | Loading tick (progress bar beat) | soft short pulse, minimal ambient |
| `sfx/progress/complete-done.mp3` | 0.8s | Completion confirmation (step done) | two ascending satisfying tones |
| `sfx/progress/generate-start.mp3` | 0.8s | AI generation start | soft rising shimmer, magical whoosh |

### 💥 Impact (brand / impact)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/impact/logo-reveal.mp3` | 0.7s | Logo impact (retained from v7b) | logo reveal thud |
| `sfx/impact/logo-reveal-v2.mp3` | 1.5s | Longer logo impact (cinematic) | cinematic bass hit with shimmer tail |
| `sfx/impact/brand-stamp.mp3` | 1.0s | Stamp impact (certification, sealing) | rubber stamp thud, paper contact |
| `sfx/impact/drop-thud.mp3` | 0.7s | Object dropping (insertion, placement) | heavy thud, wood surface contact |

### ✨ Magic (AI transformation)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/magic/sparkle.mp3` | 0.8s | Magical sparkle (AI highlight, surprise) | bright twinkling stars, fairy dust |
| `sfx/magic/ai-process.mp3` | 1.2s | AI processing sound (thinking state) | modulating digital hum with shimmer |
| `sfx/magic/transform.mp3` | 1.0s | Transform transition (morph effect) | rising shimmer whoosh with sparkle tail |

### 💻 Terminal (command line)

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/terminal/command-execute.mp3` | 0.5s | Command executed | crisp digital beep with tick, hacker ui |
| `sfx/terminal/output-appear.mp3` | 0.6s | Output appears | rapid digital ticks, retro printout |
| `sfx/terminal/cursor-blink.mp3` | 0.5s | Cursor blink | subtle soft digital pulse, rhythmic |

---

## Recommended pairings by scene

### 💻 Terminal interaction demo
```
type (0.5s) → enter (0.5s) → command-execute (0.5s) → output-appear (0.6s)
```
Loop element: `cursor-blink` as the ambient sound during idle.

### 🃏 Card selection flow
```
hover-subtle (0.5s, UI hover) → click-soft (0.5s, click) → card-snap (0.5s, snap)
```
Or advanced version: `card-flip` for front-back switching.

### 🤖 AI generation full flow
```
generate-start (0.8s, start) → ai-process (1.2s, processing) → sparkle (0.8s, flash) → complete-done (0.8s, done)
```
On error, replace `complete-done` with `error-tone`.

### 🎬 Logo Reveal (brand moment)
```
whoosh-fast (0.6s, buildup) → logo-reveal-v2 (1.5s, landing) → sparkle (0.8s, tail)
```
Simple version: `whoosh → logo-reveal` (the v7b two-piece set directly).

### 📱 UI interaction demo (mobile)
```
tap-finger (0.5s, tap) → slide-in (0.6s, panel slides in) → toggle-on (0.5s, toggle)
```
After completion: `success-chime` or `notification-pop`.

### 📊 Data visualization / dashboard
```
loading-tick (0.5s, beat) × N → complete-done (0.8s, data arrives) → achievement (1.5s, stunning landing)
```

### 🎯 Form submission flow
```
click-soft (0.5s) → loading-tick ×2 (1.0s) → success-chime (1.0s)
```
Failure branch: `error-tone (0.7s)`.

### 🪄 Magic Transform scenario
```
whoosh-fast (0.6s) → transform (1.0s) → sparkle (0.8s)
```
Suitable for: element morphing, before/after effect comparison, "AI rewrite" demos.

---

## Usage conventions

### Volume recommendations (from the audio dual-track system in apple-gallery-showcase.md)
- **SFX main track**: `1.0` (no attenuation)
- **BGM background track**: `0.4 ~ 0.5` (SFX cuts through clearly)
- **Multiple SFX overlay**: use `amix=inputs=N:duration=longest:normalize=0` to preserve dynamic range

### ffmpeg mixing template
```bash
# Single SFX aligned to a time point:
ffmpeg -i video.mp4 -itsoffset 2.5 -i sfx/ui/click.mp3 \
  -filter_complex "[0:a][1:a]amix=inputs=2:duration=longest:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4

# Multiple SFX + BGM:
ffmpeg -i video.mp4 \
  -itsoffset 1.0 -i sfx/transition/whoosh-fast.mp3 \
  -itsoffset 1.6 -i sfx/impact/logo-reveal-v2.mp3 \
  -i bgm.mp3 \
  -filter_complex "[3:a]volume=0.4[bgm];[0:a][1:a][2:a][bgm]amix=inputs=4:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4
```

### Selection decision tree
1. **Tactile action** (typing/click/swipe) → `keyboard/` or `ui/`
2. **Element entry/exit** → `transition/`
3. **Container-level operations** (cards/modals) → `container/`
4. **State feedback** (success/failure/notification) → `feedback/`
5. **Progress / time elapsing** → `progress/`
6. **Brand landing / important moments** → `impact/`
7. **AI magic / transformation** → `magic/`
8. **Command line / code demo** → `terminal/`

### Avoid SFX overlap pile-up
- At any given time, `max 2 concurrent SFX`
- When BGM drops below 0.3, you can have 3
- Clear other SFX during brand impact (leave 0.2s of silence before the landing)

---

## Prompt-writing principles (for reuse)

Reference style: `apple keynote, tight, minimal, no reverb unless ambient, crisp, elegant`

**Three elements of a good prompt**:
1. **Physical sound description**: what object, what action ("mechanical keyboard single key press")
2. **Texture / style qualifier**: apple-style / ios-style / cinematic / retro
3. **Counter-example exclusion**: no reverb / clean studio / minimal

❌ "click sound"
✅ "crisp ui button click, clean modern interface sound, apple-style, high pitched"

❌ "magic"
✅ "bright twinkling stars sound, high pitched glittery chime, fairy dust"

---

## See also
- Audio dual-track system and ffmpeg mixing: `apple-gallery-showcase.md`
- Original generation script: `/tmp/gen_sfx_batch.sh` (one-shot batch generator)
